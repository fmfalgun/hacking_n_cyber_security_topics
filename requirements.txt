# ============================================================================
# Wireless Protocol Security Research - Python Dependencies
# ============================================================================
# This file contains ALL Python dependencies for WiFi, BLE, Zigbee, and LoRa
# attack implementations and infrastructure tools.
#
# Installation:
#   pip install -r requirements.txt
#
# For protocol-specific installations, use extras:
#   pip install -e .[ble]      # BLE only
#   pip install -e .[wifi]     # WiFi only
#   pip install -e .[zigbee]   # Zigbee only
#   pip install -e .[lora]     # LoRa only
#   pip install -e .[ml]       # ML/Dataset tools
#   pip install -e .[all]      # Everything
# ============================================================================

# ----------------------------------------------------------------------------
# Core Dependencies (Required for all protocols)
# ----------------------------------------------------------------------------
scapy>=2.5.0              # Packet crafting and analysis
pyshark>=0.6.0            # Wireshark integration
pandas>=2.0.0             # Data manipulation
numpy>=1.24.0             # Numerical operations
pyyaml>=6.0               # Configuration files
python-dateutil>=2.8.2    # Date/time utilities
colorama>=0.4.6           # Colored terminal output
click>=8.1.0              # CLI framework
tqdm>=4.65.0              # Progress bars

# ----------------------------------------------------------------------------
# WiFi Protocol (802.11)
# ----------------------------------------------------------------------------
# Note: WiFi attacks primarily use scapy with raw sockets
# Additional utilities for network management
pyric>=0.1.6.3            # WiFi interface management (Python Radio Interface Controller)
netifaces>=0.11.0         # Network interface enumeration
# Note: aircrack-ng suite required (install via: apt install aircrack-ng)

# ----------------------------------------------------------------------------
# BLE (Bluetooth Low Energy)
# ----------------------------------------------------------------------------
bleak>=0.21.0             # Cross-platform BLE library (async)
bluepy>=1.3.0             # BLE scanner/peripheral (Linux only)
pybluez>=0.23             # Bluetooth Classic + some BLE support
# gattlib>=0.20150805     # GATT library (install separately if needed)
# Note: BlueZ required (install via: apt install bluez bluez-tools)

# ----------------------------------------------------------------------------
# Zigbee (IEEE 802.15.4)
# ----------------------------------------------------------------------------
# killerbee>=3.0.0        # Zigbee attack framework (install separately)
# Note: KillerBee requires manual installation from GitHub
# git clone https://github.com/riverloopsec/killerbee
# pip install -e killerbee/
# Also requires: libgcrypt-dev, libusb-1.0-0-dev

# ----------------------------------------------------------------------------
# LoRa/LoRaWAN
# ----------------------------------------------------------------------------
pyserial>=3.5             # Serial communication with LoRa modules
pycryptodome>=3.18.0      # AES encryption for LoRaWAN
# pyLoRa>=0.4.0           # LoRa library (install if available)
# Note: GNU Radio required for SDR-based LoRa
# apt install gnuradio gr-lora

# ----------------------------------------------------------------------------
# Traffic Capture & Analysis
# ----------------------------------------------------------------------------
dpkt>=1.9.8               # Fast packet parsing
pcapy-ng>=1.0.9           # libpcap wrapper (alternative to scapy)

# ----------------------------------------------------------------------------
# Dataset Pipeline & ML
# ----------------------------------------------------------------------------
scikit-learn>=1.3.0       # Machine learning algorithms
tensorflow>=2.13.0        # Deep learning framework
# torch>=2.0.0            # PyTorch (alternative to TensorFlow)
matplotlib>=3.7.0         # Plotting and visualization
seaborn>=0.12.0           # Statistical visualizations
joblib>=1.3.0             # Model serialization
pyarrow>=12.0.0           # Parquet file support
fastparquet>=2023.4.0     # Fast Parquet I/O

# ----------------------------------------------------------------------------
# Testing & Development
# ----------------------------------------------------------------------------
pytest>=7.4.0             # Testing framework
pytest-cov>=4.1.0         # Code coverage
pytest-asyncio>=0.21.0    # Async test support
black>=23.0.0             # Code formatting
flake8>=6.0.0             # Linting
mypy>=1.4.0               # Type checking
ipython>=8.14.0           # Enhanced REPL
jupyter>=1.0.0            # Notebooks for analysis

# ----------------------------------------------------------------------------
# Utilities
# ----------------------------------------------------------------------------
requests>=2.31.0          # HTTP requests
tabulate>=0.9.0           # Pretty table printing
rich>=13.4.0              # Rich terminal formatting
psutil>=5.9.0             # System monitoring
